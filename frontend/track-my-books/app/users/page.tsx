export default async function Page() {
  // const users = await getUsers();

  return (
    <div>
      <h1>aa</h1>
      {/* {users.map((u: string) => (
        <div key={u}>{u}</div>
      ))} */}
    </div>
  );
}
